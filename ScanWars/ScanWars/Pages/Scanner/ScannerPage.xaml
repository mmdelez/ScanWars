<?xml version="1.0" encoding="utf-8" ?>
<pages:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:pages="clr-namespace:ScanWars.Pages"
                xmlns:zxing="clr-namespace:ZXing.Net.Mobile.Forms;assembly=ZXing.Net.Mobile.Forms"
                x:Class="ScanWars.Pages.Scanner.ScannerPage">
    <ContentPage.Content>
        <StackLayout>
            <!--Grid for camera view-->
            <Grid HorizontalOptions="Center"
                      x:Name="zxingContainer"
                      VerticalOptions="Center"
                      HeightRequest="500"
                      WidthRequest="500"
                      IsVisible="{Binding CameraAccessGranted}">

                <!--Scanner must be created in code behind
                    because on Android < 8.0 the scanner stays black
                    when the app come back from background.-->
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</pages:BasePage>